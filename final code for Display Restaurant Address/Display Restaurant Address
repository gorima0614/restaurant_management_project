Step 1: Define the Address Model
# models.py
from django.db import models

class RestaurantLocation(models.Model):
    address = models.CharField(max_length=255)
    city = models.CharField(max_length=100)
    state = models.CharField(max_length=100)
    zip_code = models.CharField(max_length=20)

    def __str__(self):
        return f"{self.address}, {self.city}, {self.state} - {self.zip_code}"

Step 2: Apply Migrations
python manage.py makemigrations
python manage.py migrate

Step 3: Update the Homepage View
# views.py
from django.shortcuts import render
from .models import RestaurantLocation

def home(request):
    location = RestaurantLocation.objects.first()  # Assume only one location for now
    return render(request, 'home.html', {'location': location})


Step 4: Update the Homepage Template
<!-- templates/home.html -->
<!DOCTYPE html>
<html>
<head>
    <title>Welcome to Our Restaurant</title>
</head>
<body>
    <h1>Welcome to Our Restaurant</h1>

    <h2>Our Address</h2>
    {% if location %}
        <p>
            {{ location.address }}<br>
            {{ location.city }}, {{ location.state }} - {{ location.zip_code }}
        </p>
    {% else %}
        <p>Address not available.</p>
    {% endif %}
</body>
</html>


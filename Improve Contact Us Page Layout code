Step 1 : Django view and form setup
fom django.shortcuts import render, redirect
from django.core.mail import send_mail
from .forms import ContactForm

def contact_us(request):
    if request.method == 'POST':
       form = ContactForm(request.POST)
       if form.is_valid():
       #Handle form submission 
       #send_mail(...) or form.save()
       return redirect('contact_success')
    else:
       form = ContactForm()
    return render(request, 'contact.html', {'form': form})

forms.py
from django import forms

class ContactForm(forms.Form):
   name = forms.CharField(max_length=100, label='Your Name')
   email = forms.EmailField(label='Your Email')
   message = forms.CharField(widget=forms.Textarea, label='Message')


Step 2 : contact.html - improved layout with basic css 
<!--templates/contact.html-->
{% load static %}
<!DOCTYPE html>
<html lang="en">
<head>
   <meta charset="UTF-8">
   <title>Contact Us</title>
   <link rel="stylesheet" href="{% static 'css/contact.css' %}">
</head>
<body>
    <div class="contact-containers">
        <h2>Contact Us</h2>
        <form method="post" class="contact-form">
            {% csrf_token %}
            {{ form.non_field_errors }}

            <div class="form-group">
                {{ form.name.label_tag }}
                {{ form.name }}
                {{ form.name.errors }}
            </div>

            <div class="form-group">
                {{ form.email.label_tag }}
                {{ form.email }}
                {{ form.email.errors }}
            </div>

            <div class="form-group">
                {{ form.message.label_tag }}
                {{ form.message }}
                {{ form.message.errors }}
            </div>

            <button type="submit">Send Message</button>
        </form>
    </div>
</body>
</html>

Step 3 : Basic CSS Styling
/* static/css/contact.css */

body {
    font-family: Arial, sans-serif;
    background: #f9f9f9;
    padding: 30px;
}

.contact-container {
    max-width: 600px;
    margin: 0 auto;
    padding: 25px;
    background-color: #fff;
    border-radius: 10px;
    box-shadow: 0 0  10px rgba(0,0,0,0.1);
}

.contact-container h2 {
    text-align: center;
    margin-bottom: 20px;
}

.contact-form .form-group {
    margin-bottom: 15px;
}

.contact-form label {
    display: block;
    font-weight: bold;
    margin-botton: 5px;
}

.contact-form input[type="text"],
.contact-form input[type="email"],
.contact-form textarea {
    width: 100%
    padding: 10px;
    border: 1px solid #ddd;
    border-radius: 5px;
    box-sizing: border-box;
}

.contact-form button {
    backgroung-color: #4CAF50;
    color: white;
    padding: 12px 20px;
    border-radius: 5px;
    width: 100%
    cursor: pointer;
    font-size: 16px;
}

.contact-form button:hover {
    backgroung-color: $45a049;
}

Step 4 : Make sure static files are set up
#settings.py
STATIC_URL = '/static/'

in your template
{% load static %}
<link rel="stylesheet" href="{% static 'css/contact.css' %}">



  
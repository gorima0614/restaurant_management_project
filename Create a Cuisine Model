from django.db import models

class Cuisine(models.Model):
    name = models.CharField(max_length=100, unique=True)

    def __str__(self):
        return self.name

from django.contrib import admin
from .models import Cuisine

@admin.register(Cuisine)
class CuisineAdmin(admin.ModelAdmin):
    list_display = ('id', 'name')  # shows ID and Name in the admin list view
    search_fields = ('name',)      # adds a search bar for cuisines


Next Steps

Save both files.
Run these commands in your terminal:
python manage.py makemigrations home
python manage.py migrate
Create a superuser (if you don’t have one yet):
python manage.py createsuperuser
Start your development server:
python manage.py runserver
Go to http://127.0.0.1:8000/admin/
 → log in → you’ll see “Cuisines” listed in the admin sidebar.

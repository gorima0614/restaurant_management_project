Step 1 : Add the view in views.py
#restaurant/views.py
from django.shortcuts import render

def gallery(request):
   #Hardcoded image URLs for now
   images = [
       "https://exmaple.com/image1.jpg",
       "https://exmaple.com/image2.jpg",
       "https://exmaple.com/image3.jpg",
       "https://exmaple.com/image4.jpg",
   ]
   return render(request, "gallery.html", {"images": images})


Step 2 : Create the template gallery.html
{% load static %}
<!DOCTYPE html>
<html>
<head>
    <title>Restaurant Gallery</title>
    <link rel="stylesheet" href="{% static 'css/style.css' %}">
    <style>
       body {
           font-family: Arial, sans-serif;
           background: #fafafa;
           text-align: center;
       }
       h1 {
           margin-top: 20px;
       }

       .gallery {
          display: grid;
          grid-template-columns: repeat(auto-fll, minmax(250px, 1fr));
          gap: 15px;
          padding: 20px;
       }

       .gallery img {
          width: 100%;
          border-radius: 10px;
          box-shadow: 0px 4px 10px rgba(0,0,0,0.1);
          transition: transform 0.3s ease;
       }

       .gallery img:hover {
        transform: scale(1.05);
       }
    </style>
</head>
<body>
    <h1>Restaurant Gallery</h1>
    <div class="gallery">
       {% for image in images %}
           <img src="{{ image }}" alt="Restaurant Image"?
       {% endfor %}
    </div>
</body>
</html>


Step 3 : Add URL Mapping
from django.urls import path
from . import views

urlpatterns = [
    path('gallery/', views,gallery, name='gallery'),
]


Step 4 : Make sure stattic files are configured
STATIC_URL = '/static/'
STATICFILES_DIRS = [BASE_DIR / "static"]

MEDIA_URL = '/media/'
MEDIA_ROOT = BASE_DIR / "media"


Step 5 : Test it 
python manage.py runserver

Go to http://localhost:8000/gallery/
You should see your harcoded restaurant images in a responsive grid.


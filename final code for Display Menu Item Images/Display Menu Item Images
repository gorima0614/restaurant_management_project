1. Ensure Your MenuItem Model Includes the Image Field
# models.py
class MenuItem(models.Model):
    name = models.CharField(max_length=100)
    description = models.TextField(blank=True)
    price = models.DecimalField(max_digits=6, decimal_places=2)
    image = models.ImageField(upload_to='menu_images/', blank=True, null=True)

    def __str__(self):
        return self.name

2. Pass Menu Items to the Template
# views.py
from .models import MenuItem

def menu_view(request):
    menu_items = MenuItem.objects.all()
    return render(request, 'menu.html', {'menu_items': menu_items})

def home(request):
    # other logic...
    menu_items = MenuItem.objects.all()
    context = {
        # existing context...
        "menu_items": menu_items,
    }
    return render(request, 'home.html', context)

3. Update Your Template (menu.htmlc or home.html) <!-- templates/menu.html or templates/home.html -->
<h1>Our Menu</h1>

<div style="display: flex; flex-wrap: wrap; gap: 20px;">
  {% for item in menu_items %}
    <div style="border: 1px solid #ddd; padding: 10px; width: 250px;">
      {% if item.image %}
        <img src="{{ item.image.url }}" alt="{{ item.name }}" style="width: 100%; height: auto;" />
      {% endif %}
      <h3>{{ item.name }}</h3>
      <p>{{ item.description }}</p>
      <p><strong>₹{{ item.price }}</strong></p>
    </div>
  {% empty %}
    <p>No menu items available.</p>
  {% endfor %}
</div>

4. Ensure Media is Served Correctly
MEDIA_URL = '/media/'
MEDIA_ROOT = BASE_DIR / 'media'

from django.conf import settings
from django.conf.urls.static import static

urlpatterns = [
    # your app URLs
] + static(settings.MEDIA_URL, document_root=settings.MEDIA_ROOT)

5. Upload Images via Admin

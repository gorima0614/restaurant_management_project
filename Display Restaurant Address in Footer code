Step 1 : Add to settings.py
#settings.py
RESTAURANT_ADDRESS = "123 Food Street, Flavour Town, India"

Step 2 : Pass it to all templates via context_processors.py
#your_app/context_processors.py
from django.conf import settings

def restaurant_address(request):
    return {
        'RESTAURANT_ADDRESS': settings.RESTAURANT_ADDRESS
    }

Step 3 : Register in settings.py
TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [BASE_DIR / "templates"],
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.debug',
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
                'your_app.context_processors.restaurant_address',
            ],
        },
    },
]

Step 4 : Add to footer.html
<footer> 
    <p>Address: {{ RESTAURANT_ADDRESS }}</p>
</footer>

